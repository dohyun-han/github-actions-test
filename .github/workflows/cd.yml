name: cd

on: [ pull_request ]

jobs:
  push-docker-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Take tag version and push tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Java17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      #      - name: backend environment setting
      #        run: |
      #          touch .env.production
      #          echo -e ${{ secrets.BACKEND_PRODUCTION_ENVIRONMENT }} > .env.production

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v2

      - name: Docker registry login
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.access_token }}

      - name: Build with Gradle
        run: chmod +x gradlew && ./gradlew bootJar

      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          tags: |
            ghcr.io/${{ github.repository_owner }}/github-actions-test:latest
            ghcr.io/${{ github.repository_owner }}/github-actions-test:${{ steps.tag_version.outputs.new_tag }}
          push: true

  cd:
    needs: [push-docker-image]
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: copy docker compose & config file into production server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.BACKEND_PROD_REMOTE_IP }}
          username: ${{ secrets.BACKEND_PROD_REMOTE_SSH_ID }}
          password: ${{ secrets.BACKEND_PROD_REMOTE_ADMIN_KEY }}
          port: ${{ secrets.BACKEND_PROD_REMOTE_SSH_PORT }}
          source: './docker-compose.prod.yml,./scripts/deploy.sh'#,backend/nginx/prod/nginx.conf,
          target: 'github-actions-test'

      - name: deploy
        uses: appleboy/ssh-action@v0.1.4
        with:
          host: ${{ secrets.BACKEND_PROD_REMOTE_IP }}
          username: ${{ secrets.BACKEND_PROD_REMOTE_SSH_ID }}
          password: ${{ secrets.BACKEND_PROD_REMOTE_ADMIN_KEY }}
          port: ${{ secrets.BACKEND_PROD_REMOTE_SSH_PORT }}
          script: |
            cd github-actions-test
            sudo chmod +x ./scripts/deploy.sh
            ./scripts/deploy.sh ${{ secrets.NCP_ACCESS_KEY }} ${{ secrets.NCP_SECRET_KEY }} ${{ secrets.NCP_DOCKER_REGISTRY }} ${{ secrets.BACKEND_PRODUCTION_ENVIRONMENT }}